============
Installation
============

At the command line::

    $ easy_install nodefactinfo

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv nodefactinfo
    $ pip install nodefactinfo